server:
  port: 9003

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MilenaTrajanoska/cloud-config
          clone-on-start: true